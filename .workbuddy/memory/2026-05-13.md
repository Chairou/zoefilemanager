- 11:16 RemoteDialog 表单宽度对齐：所有输入控件（QLineEdit/QSpinBox/含浏览按钮的 keyRow 复合行）统一固定宽度 200px，与协议下拉框 m_protocolCombo（SFTP/SMB）宽度对齐。kFieldWidth=200 + lambda fixWidth 设 setMinimumWidth/setMaximumWidth 双向锁定。keyRow 内的浏览按钮固定 24px，让整行加起来等于 200px。视觉上"连接信息"区块的右列变成一根整齐的列。
- 11:20 RemoteDialog 给所有连接信息输入控件加 tooltip：协议下拉框/Host/Port/Username/Password/私钥路径/浏览按钮/Passphrase/Share/Workgroup 都通过 setToolTip 设置详细提示，包含示例和企业域 AD 场景（DOMAIN\\user、user@domain、TENCENT 等）。tooltip 文本走 T() 查表支持实时语言切换；放在 retranslate() 里集中管理，避免与协议联动 onProtocolChanged 互相覆盖。tooltip 比 placeholder 信息更全（多行 \n 分段），鼠标悬停 ~1s 后弹出。
- 11:23 RemoteDialog tooltip 改中文：之前的英文 key 在 i18n 表里没词条，T() fallback 显示英文原文。直接把 key 改成中文（多行 \n 分段，包含示例和企业 AD 域场景）。后续要做英文版只需在 I18n.cpp 里补反向映射。**踩坑**：tooltip 文本里一开始用了中文双引号 "…"，C++ 编译器报错 'character <U+2026> not allowed in an identifier'，因为中文 "..." 不是字符串边界，被识别成 raw 标识符。修复：改成 ASCII 双引号 \" 转义。
- 11:47 ShortcutBar 升级为"快捷管理器"：支持目录分组（一层结构，目录内只放快捷方式不再嵌套目录）。Header 加第二个按钮 📁（新建目录）放 + 按钮左侧。数据模型 ShortcutItem 增加 isFolder/expanded/children 字段。目录头按钮显示 ▼/▶ 折叠箭头 + 📁 emoji + 名称 + 子项数 (N)，点击切换 expanded。子项缩进显示（4 空格 prefix）。右键菜单：快捷方式 → Edit/Remove；目录 → Add shortcut here / Rename folder / Remove folder（删除目录会连同其下快捷方式一起删，需确认）。持久化：QSettings shortcuts[i] 增加 isFolder/expanded 字段 + 嵌套 children[] 数组；旧扁平结构（只 name+path）按 isFolder=false 兼容读取。saveToSettings 先 s.remove("shortcuts") 再写，避免上次的 children 残留污染。
- 14:09 ShortcutBar 点击本地目录后，左侧 DirectoryTree 跟随同步：在 MainWindow.cpp line 134 的 shortcutActivated lambda 里，navigateTo(path) 之后显式调用 m_dirTree->revealPath(canonical) + highlightPath(canonical, m_activePane)，让点击快捷方式与点击树节点的视觉联动完全一致（展开+滚动+高亮）。canonical 取自 activePanel()->currentPath() 而非原始 path 参数，避免尾随斜杠/符号链接造成不匹配。远程 URL（sftp/smb）不在树中，DirectoryTree::revealPath 会静默跳过，行为安全。
- 14:21 上一条 hotfix 不够：用户反馈树仍然不联动。根因——DirectoryTree::m_pathToItem 的 key 是规范化形式（无尾随 / / 无 ~），但快捷方式存的 path 常带 `~/` 或尾随 `/`，expandToPath 用 split SkipEmptyParts 累加 key 时只能匹配父链，到精确目标节点 m_pathToItem.find(rawPath) 找不到 → setCurrentItem/scrollToItem 被跳过。修复两处：(1) DirectoryTree.cpp 顶部新增 normalizeTreeKey() 静态函数（展开 ~/ + 折叠 // + chop 尾随 /），highlightPath/expandToPath/revealPath 入口都先 normalize；(2) MainWindow shortcutActivated lambda 改为直接用 shortcut 自身 path 作为同步源（不再读 currentPath() 回环值——navigateTo 在目录不存在时不会更新 currentPath，但联动应该按用户预期位置展开）。
- 14:38 ShortcutBar 增补 SFTP/SMB 凭证字段：原本只录主机/路径，缺账号密码。本次 ShortcutItem 新增 username/password/workgroup 三字段；AddShortcutDialog 增加 m_pwdEdit（SFTP）+ m_smbUserEdit/m_smbPwdEdit（SMB），applyTypeUI 按 type 显隐；对外暴露 setCredentials() / username() / password() / workgroup() 接口。密码不写入 path URL（避免 tooltip 暴露），单独通过 QSettings password_obf 字段存储——用 XOR(8字节 key) + Base64 双层简单混淆，仅防止 INI 文件肉眼可读，**非强加密**，将来要升级到 macOS Keychain 可平滑替换 obfuscate/deobfuscate 函数实现。Tooltip 明确警告"以本地混淆形式保存（非强加密），勿在共享设备保存敏感账号"。所有 add/edit 路径（onAddShortcutClicked / editTopLevel / addChildToFolder / editChildOfFolder）和持久化（loadFromSettings/saveToSettings）都同步更新。
- 14:54 SMB 快捷点击不连接的 bug 修复：原 ShortcutBar 只 emit shortcutActivated(path)，MainWindow 收到 path 后 navigateTo("smb://...")，但 router 上根本没挂 SmbClient session 所以是空导航。修复方案：(1) ShortcutBar 新增信号 shortcutActivatedFull(const ShortcutItem&)，与 path-only 版同时 emit，按值快照捕获 ShortcutItem 不依赖 m_items 生命周期；(2) MainWindow.h 新增私有方法 ensureRemoteMountedFor(item)，按 item.type 分支（smb/sftp）从 path URL 反解 host/share/port/user，结合 item 内的 username/password/workgroup 构造 RemoteConnection，调 SmbClient/SftpClient::connectAndAuth + FileSystemRouter::mountSmb/mount，已挂同 mount prefix 时复用不重复连接；(3) MainWindow lambda 改听 shortcutActivatedFull，远程类型先 ensureRemoteMountedFor 失败 return（弹 QMessageBox + statusBar），成功后 navigateTo(path + "/")；本地类型走原路径。SMB mount prefix 用 SmbClient::buildUrl(host, share)；SFTP mount prefix 用 user@host:port，与 onRemoteConnect 保持一致。
- 15:13 SMB 快捷 ECONNABORTED(53) 修复（老问题，URL 编码 + 子路径丢失）：用户报错 `opendir smb://tencent.com/tfs failed: Software caused connection abort (errno=53)`。根因——14:54 版本 ensureRemoteMountedFor SMB 分支用 `slash2 = tail.indexOf('/')` 切了 share 第一段后**直接丢弃 sub-path**，传给 SmbClient::connectAndAuth 的 conn.share 只剩 "tfs" → stage-1 opendir 在 DFS namespace 主机 tencent.com 直接 abort（DFS 没有 stage-2 完整路径就不会触发 referral 到真实服务器 NS-SZY01）→ ECONNABORTED；同时 mount prefix `buildUrl(host, "tfs")` 与浏览路径不一致，且 item.path 中文未 percent-encode 而 SmbClient entry.path 已编码 → router 前缀不命中。修复：(1) SMB 分支 fullShare 保留整段（含 sub），传给 conn.share 让 SmbClient 内部按 "/" 拆 firstShare/restPath 走两步 DFS 流程；(2) ensureRemoteMountedFor 加 outNavUrl out-param 回传"已编码 mount URL"（用 SmbClient::buildUrl 重新拼）；SFTP 同步从 path 取出 remotePath 拼 mount+remotePath；(3) lambda 改用 outNavUrl 而非 item.path 来 navigateTo，与 router 内部字面对齐。教训：DFS 必须 stage-2，conn.share 不可被截断；ShortcutItem.path（用户可读）和 mount prefix（router key）是两套字面，**永远用 buildUrl 重建 mount**，不要直接拿 item.path。
- 15:55 ShortcutBar 右键菜单透明 + 增加选中态：(1) macOS 暗色下 QMenu 默认走 vibrancy 半透明，文字与桌面互相干扰看不清——新增 namespace 内静态函数 applyMenuStyle(QMenu*)，先 setAttribute(WA_TranslucentBackground, false) 强制 Qt 自绘，再用 Nord 配色 stylesheet 显式覆盖（背景 #2E3440 / hover #5E81AC / separator #434C5E）。两处 customContextMenuRequested 都调 applyMenuStyle。(2) ShortcutBar.h 新增 m_selectedPath 成员；左键 click 和右键弹菜单时都更新 m_selectedPath 并 rebuildButtons → makeShortcutButton 按 selected 渲染 Nord Frost 蓝底(#5E81AC) + 白字(#ECEFF4) + 左侧 3px 强调条（颜色按 type 取 ts.accent，与 hover 态保持视觉连贯）。坑：右键时 rebuildButtons 会析构调用方所在的 btn，所以右键路径分两种——若 path != m_selectedPath 则先选中再用 QCursor::pos() 弹菜单（不再触碰悬空 btn 指针）；若已选中走原 btn->mapToGlobal(pos) 路径。
- 16:08 ShortcutBar 三个进阶需求：(1) folder 选中态高亮：ShortcutBar.h 新增 m_selectedFolderIndex 同时承担"folder 自身高亮"+"新增目标 folder"两个职责；makeFolderButton 当 m_selectedFolderIndex == folderIndex && m_selectedPath.isEmpty() 时渲染 Frost 蓝底 + 白字 + 黄色 #EBCB8B 3px 左强调条 + bold（与 shortcut 选中态形成视觉层级区分）。folder 单击 = 清 m_selectedPath + 设 m_selectedFolderIndex + toggle expand 三件套。(2) 智能落点：onAddShortcutClicked 检查 m_selectedFolderIndex 有效则走 addChildToFolder(m_selectedFolderIndex)，否则走 addTopLevel；shortcut 子项选中时 folderIndex 回写到 m_selectedFolderIndex（顶层 shortcut 是 -1），所以"选中 folder 子项 → 点 + → 加到同一 folder"也成立。(3) 右键菜单加"Add folder..."：shortcut 右键和 folder 右键都加，统一调 onAddFolderClicked → 顶层创建（folder 不嵌套约束保留）。removeTopLevel 加防御性逻辑：被删的是当前选中 folder 则清空，删除的索引在选中前面则 --m_selectedFolderIndex。
- 17:01 ShortcutBar 智能落点 — 用户反馈"选中 folder 后点右上角 + 仍落顶层"。代码核查：onAddShortcutClicked / onAddFolderClicked 已经走 selectedTargetFolderPath() → addShortcutUnder/addFolderUnder（src/ShortcutBar.cpp:1180-1190），逻辑早在 16:08 + 16:45 重构后就是对的：选中 folder → 落该 folder；选中 folder 子项 → 落同 folder（去末位）；空选 → 顶层。原因实测假阴性 = 用户运行的是 16:44 的旧 .app，未重启。`touch src/ShortcutBar.cpp && cmake --build build -j` 强制重链并 macdeployqt 重打包，产物刷到 17:40，重启后即生效。无代码改动。教训：每次改完务必重启 app；将来类似行为缺失先看产物时间戳。
- 16:45 ShortcutBar 概念重构：明确区分"快捷目录（Shortcut Folder，可嵌套的容器）" vs "本地目录（Local shortcut，叶子，不可嵌套）"。(1) ShortcutBar.h 头部注释完全重写澄清概念；ShortcutItem.children 注释改为"可同时装叶子和嵌套快捷目录"；私有方法整体替换为 path 化签名：itemAt(QVector<int>)/removeAtPath/editAtPath/addShortcutUnder/addFolderUnder/toggleExpandedAtPath/renderItems/selectedTargetFolderPath，删除原 removeTopLevel/editTopLevel/toggleFolderExpanded/addChildToFolder/removeChildFromFolder/editChildOfFolder。成员合并：仅留 m_selectedPath（QVector<int>），撤销 m_selectedFolderIndex；selectedTargetFolderPath() 派生：选中 folder → 返其 path，选中 shortcut → 返父 folder path（去掉末位）。(2) ShortcutBar.cpp：TypeStyle 增 useSystemDirIcon，local 留空 emoji 改用 qApp->style()->standardIcon(QStyle::SP_DirIcon) + setIconSize{16,16}（与 DirectoryTree 同根同源），sftp 🌐 / smb 🖥 emoji 保留；renderItems 递归遍历 items，path 累积，indent+1 下传；itemAt(path) 按 vector 逐层下钻（中间节点必须 isFolder）；removeAtPath 用 std::function lambda 递归数后代叶子做提示文案；选中态前缀匹配清理或同级索引前移；右键菜单 folder 上加"Add shortcut folder..."直接嵌套到该 folder。(3) 持久化重构为递归读写：抽出静态 helper readArrayRecursive/writeArrayRecursive，folder 写 children 子数组（哪怕空）也写以保持结构清晰；inferTypeFromPath 提升为静态函数复用。向后兼容：旧扁平 children（无嵌套）被新代码当作"folder 但不再含子 folder"读入。编译干净通过。








- 18:19 ShortcutBar local 类型缩进对齐 SMB/SFTP：原 local 走 setIcon(SP_DirIcon)，按钮 icon 区域把 label 文本整体右推（icon 16px + spacing），与 SMB/SFTP（emoji 拼进 label 文本）的文本起点不一致 → 在快捷目录内子项缩进时视觉偏移很明显。修复：TypeStyle 移除 useSystemDirIcon 字段，local 改为用 📂 emoji（U+1F4C2，开口文件夹）走纯文本路径，与"快捷目录"📁（U+1F4C1，合口）emoji 区分语义。makeShortcutButton 三类型统一 `prefix + emoji + name` 渲染，indent 缩进文本起点完全对齐。styleFor 简化为两字段（iconUtf8/accent）。
- 18:39 DirectoryTree 高亮颜色按 active 侧优先：bug 现象——左右面板停在同一目录时，目录树该项颜色总是后调用 highlightPath 的那侧（多数被右绿覆盖左黄），用户感觉"左右一样"。根因：两次 highlightPath 写在同一 QTreeWidgetItem 上，setForeground 后写覆盖前写。修复：(1) DirectoryTree.h 新增 m_activeSide 成员 + setActiveSide(PanelSide) 接口；(2) highlightPath 检测 sameAsOther（对侧 highlight key 等于本侧）时 colorSide = m_activeSide，按 active 侧颜色着色而非本侧；(3) clearHighlight 改为先看对侧是否仍持有该 item，若持有就重染为对侧颜色而不是清空（避免单侧 clear 把双指共享的高亮误擦）；(4) MainWindow::onPanelActivated 调用 m_dirTree->setActiveSide(side) 触发即时重染；loadSettings 末尾也补一次 setActiveSide(m_activePane)。结果：active 左面板 → 共指目录显黄 #EBCB8B；active 右面板 → 共指目录显绿 #A3BE8C；分别指向不同目录则各显本色，与 tab 颜色一致。
- 18:55 DirectoryTree 选择条背景色跟随 active tab 颜色：上一条只改了文字 foreground，但截图里"选择条"灰蓝色仍是 MainWindow 全局样式表 `QTreeWidget::item:selected { background: rgba(136,192,208,0.28) }`（Nord Frost 蓝）硬编码。修复：DirectoryTree.h 新增 `applyActiveAccentStyle()`；DirectoryTree.cpp 实现按 m_activeSide 取 m_leftColor(#EBCB8B 黄) / m_rightColor(#A3BE8C 绿)，rgba 0.28 透明度做选择条背景，同时定义 hover 0.10 半透；通过本控件局部 setStyleSheet 覆盖 MainWindow 全局规则（Qt 局部 stylesheet 优先级高于全局）。同时定义 `:selected:!active` 同色，避免树失焦时回到默认蓝。在构造函数末尾首次调用一次确保启动即生效；setActiveSide 末尾再调一次保证切换面板时刷新。验证：cmake --build build -j 编译通过，可重启 app 观察。
- 19:17 FilePanel 文件列表激活高亮 + 左黄右绿配色：(1) FileListView 新增 setActiveHighlight(bool, QColor)，走局部 stylesheet 写 `QTableWidget::item:selected { background-color: rgba(R,G,B,28%) }` 覆盖 MainWindow 全局 #88C0D0 蓝；非 active 时背景 transparent + 文字 #D8DEE9，让"焦点不在我这边"一眼可见。(2) FilePanel 新增 setActive(bool)：把状态广播到所有 tab 的 fileList，accent 色由 m_accentColor（左 #EBCB8B 黄 / 右 #A3BE8C 绿）注入；createTab 末尾调一次让新建 tab 立刻继承当前 panel.active 状态。(3) MainWindow::onPanelActivated 内成对调用 leftPanel.setActive(side==Left) / rightPanel.setActive(side==Right) 实现互斥；loadSettings 末尾也补调一次保证启动后初始化即生效。教训：多 tab 控件下 setActive 不能只动 currentTab().fileList，必须遍历所有 tab，否则切 tab 时新 tab 仍是默认蓝高亮。
- 19:34 非激活面板"清洁视图"调整：上一版非 active 时还把 selected 行文字色改成 #D8DEE9，会和目录蓝/代码绿/配置黄等 setForeground 的文件类型色冲突，仍能看出选中痕迹。改为：非 active css 只写 `QTableWidget::item:selected { background-color: transparent }`，**不设 color** 让 setForeground 的类型色原样透出；同时补一条 `QTableWidget::item:hover { background-color: transparent }` 抹掉鼠标掠过时全局样式表会闪蓝的 hover 条。结果：非激活面板的选中行视觉与未选中行完全一致 = 真"清洁窗口"，激活时仍按左黄/右绿亮起。
- 19:40 非激活面板高亮 bug：用户截图显示右侧（非激活）qt1 行仍显示 Nord Frost 蓝高亮。根因——`FilePanel::setActive` 里 `if (m_active == active) return;` 早返回守卫坑了启动流程：m_active 默认 false，loadSettings 调 rightPanel.setActive(false) 被判 no-op，fileList 从未应用 stylesheet，落回 MainWindow 全局表的 #88C0D0 蓝。修复：删掉早返回守卫，让每次调用都强制下发 stylesheet（幂等且廉价）。教训：UI 状态同步函数尽量避免 dirty 检查早返回，除非确实需要去重；初始化路径上"默认值 == 目标值"是常见陷阱。
- 19:50 非激活高亮 bug 真·根因（守卫只是表象）：上一版即便守卫删了，非激活面板 qt1 行依然显示 Nord Frost 蓝。真根因有三层：(1) **stylesheet 特异性冲突**——FileListView 用 `QTableWidget::item:selected` 选择器去覆盖 MainWindow 全局同选择器规则，特异性相等 → Qt 级联里全局规则可能赢；(2) **属性级 vs 规则级双通道**——全局 QSS 还有 `selection-background-color: rgba(136,192,208,0.25)` 这种属性级声明，QStyle 直接读，:selected 规则盖不住；(3) **QPalette::Highlight 第三通道**——macOS 原生 QStyle 走 palette 取色绘制选中行，stylesheet 完全绕不开。三管齐下修复：① 选择器升级为 `FileListView::item:selected`（类名级特异性必胜全局 `QTableWidget::item:selected`）；② 同时写 `selection-background-color` / `selection-color` 属性级压制；③ 直接 setPalette 改 QPalette::Highlight + Inactive 组（macOS 失焦态用 Inactive），非 active 时 alpha=0 透明 + HighlightedText 设回 #D8DEE9 普通文字色避免选中变白；④ active 时 Highlight 设为带 alpha 的 accent 色。补 #include <QPalette>。教训：Qt 选中色至少有三条独立通道（stylesheet 规则 / stylesheet 属性 / QPalette），要彻底覆盖必须三管齐下，单点修补永远会被某条通道穿透。
