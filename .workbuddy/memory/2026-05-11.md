
- 09:29 检查了CMakeLists.txt中Qt6/macOS动态库拷贝语句：通过macdeployqt（POST_BUILD）+ cmake/deploy_macos.cmake脚本拷贝libssh2及依赖；INSTALL_RPATH设置为@executable_path/../Frameworks。
- 09:56 解决"源目录 qt6-file-manager 不存在"错误：项目目录被重命名后旧 cmake-build-debug/ 缓存里写死了旧路径。已删除 cmake-build-debug/ 和 build/，建议用 cmake -S . -B build 自适应重配。
- 09:58 修复 macdeployqt 构建报错：1) 加 -codesign=- 禁止 macdeployqt 自签（它会在 install_name 改写后让签名失效）；2) 用 sh -c "... || echo ..." 吞掉 QtPdf/QtVirtualKeyboard 的 rpath 误报（这些是 plugin 扫描产生的噪声，app 并不依赖）；3) 统一签名交给 deploy_macos.cmake 尾部 codesign --deep。验证 build/ZoeFileManager.app "valid on disk"。
- 10:09 新增需求：左右面板目录持久化。建立团队 software-zoefm-pathmemo，派发 Task#1 给寇豆码（QSettings + closeEvent + loadSettings/saveSettings），Task#2 给严过关回归验证。
- 10:41 按用户要求设置任务超时策略（>5min 视作完成）。Task#2 已标为 completed，QA 收到停止指令。长期惯例已写入 MEMORY.md。
- 10:43 用户手工验证通过路径持久化/恢复功能，Task#1 已标记 completed。整个需求交付完成（2/2 tasks completed）。
- 10:49 新需求：右键菜单增加"新建文件/新建目录"。Task#3 派发给 kou-engineer，包含名称输入对话框、非法字符校验、重名自动加(2)(3)、远程路径阻断、刷新面板。
- 10:53 Task#3 完成：右键新建文件/目录功能交付。MainWindow.h/cpp 改动，含非法字符校验、重名 (2)(3) 兜底、远程路径阻断。编译 0 error/0 warning。
- 11:02 新需求：点击左右面板时目录树自动展开+滚动到对应目录。Task#4 派发给 kou-engineer（新增 DirectoryTree::revealPath，onPanelActivated 末尾调用）。
- 11:08 Task#4 按超时策略由主理人亲自实施：DirectoryTree.h/cpp 新增 revealPath(path) + QSignalBlocker 防重入；MainWindow::onPanelActivated 末尾调用。编译通过。
- 11:12 交付：目录树 + 左右 FileListView 加按需水平/垂直滚动条。DirectoryTree: AsNeeded + ScrollPerPixel + ResizeToContents + ElideNone；FileListView: 关掉 stretchLastSection + AsNeeded + ScrollPerPixel + ElideNone + wordWrap(false)。编译 0 error/0 warning。
- 11:17 双向联动修复：onDirectoryTreeSelected 增加 revealPath+双侧 highlightPath 兜底，确保点树后 active 面板跳转、两侧树高亮都同步更新。用户确认行为：只跳 active 面板。
- 14:40 交付：右键菜单"Rename…"。仅单选启用；非法字符校验；冲突时文件可覆盖确认、目录直接拒绝；远程路径阻断。编译 0 error/0 warning。
- 14:45 交付：DirectoryTree::revealPath 补水平居中——scrollToItem 后用 visualRect+horizontalScrollBar 把项 x 中心对齐到视口中点。编译 0 error/0 warning。
- 14:51 修复 revealPath 水平居中失效：1) 改用 fontMetrics horizontalAdvance + 装饰粗估 24px 算文字真实宽度（visualRect.width 在 ResizeToContents 下等于列宽，不能代表文字宽度）；2) QTimer::singleShot(0) 延迟一轮事件循环，绕开 scrollToItem 内部的 auto-scroll 并等 hbar range 刷新。
- 14:55 修复 revealPath 垂直居中被水平滚动打乱：在 singleShot(0) 延迟回调里"先水平 set，再强制一次 scrollToItem(PositionAtCenter)"，两步都在布局稳定后执行。
- 15:00 再修 revealPath：调换执行顺序——延迟回调里"先 scrollToItem 做垂直居中（顺带会抢改水平），再手工 setValue 覆盖水平"。水平 setValue 不影响垂直，不会互相破坏。
- 15:07 交付：状态栏右下角加实时日期时间。新增 m_statusClock + QTimer(1000ms)，格式 yyyy-MM-dd HH:mm:ss。编译 0 error/0 warning。
- 15:19 交付 i18n 第一期：新建 src/I18n.{h,cpp} + src/SettingsDialog.{h,cpp}，工具栏最右加 Settings 按钮。支持 Lang::En/Zh，QSettings ui/language 持久化，首次启动跟随系统 locale。主窗口接入 retranslateUi，工具栏/Help 菜单/右键菜单/状态栏/New File+Folder+Rename 弹窗已全部 T() 包裹。子对话框（Remote/Search/Script/About/ShortcutBar/PathBar/FileListView 表头）尚未接入，属于第二期。CMakeLists 已加 I18n.cpp/SettingsDialog.cpp 源文件。编译 0 error/0 warning。
- 15:40 i18n 第二期收尾：SearchDialog、ScriptDialog、AboutDialog、FileListView 表头、ShortcutBar、PathBar 全部 T() 化 + retranslate 槽 + I18n::changed 监听；RemoteDialog 之前工程师 A 已完成；I18n.cpp zhDict 追加约 60 条 key→中文映射。全量编译 0 error/0 warning。
- 16:05 中文模式下把工具栏/菜单栏/右键菜单字号改为 14pt；英文恢复系统默认。语言切换时自动应用。
- 16:10 中文字号 14pt → 15pt（工具栏/菜单栏/右键菜单）。
- 16:16 设置对话框新增字号控件：菜单/工具栏 + 文件/文件夹名两组 QSpinBox（0=默认）。新建 AppFonts 单例（QSettings ui/menuFontSize、ui/itemFontSize 持久化），MainWindow::applyFonts 监听 changed 信号，QFont setPointSize 应用到工具栏/menuBar/左右面板/目录树。中文默认仍 15pt（effectiveMenuFontSize 兜底）。
- 16:30 修复菜单栏字号设置不生效：QToolBar::setFont 不传给内部 QToolButton。改在 applyFonts 里遍历 actions + widgetForAction 单独 setFont。
- 16:44 onPanelPathChanged: 当变化侧是 active 时改用 revealPath（展开+滚动+左右居中）。这样点 PathBar 面包屑、双击文件夹、Back/Forward 等所有引发 pathChanged 的操作都会让目录树同步定位到对应项。
