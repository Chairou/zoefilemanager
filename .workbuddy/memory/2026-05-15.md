# 2026-05-15 工作日志

## 修复：install_name_tool 签名失效 warning + 签名验证失败

构建时 macOS install_name_tool 输出 6 条 warning：
> changes being made to the file will invalidate the code signature in: libsmbclient.0.8.1.dylib

且 `codesign --verify --deep --strict` 报：
> a sealed resource is missing or invalid: file added: .../Contents/.macdeployqt.done

### 根因

1. **warning**：Homebrew 的 dylib 自带 ad-hoc 签名；install_name_tool 修改 LC_LOAD_DYLIB 时，旧签名 hash 不再匹配 → 输出 warning。warning 本身无害（我们后续会重签），但日志噪声大。
2. **签名验证失败**：原本 sentinel 文件 `.macdeployqt.done` 是在 codesign --deep 之后才 `touch` 创建的，导致 bundle 被签后又出现新文件 → 资源不匹配。

### 修复

**1. `cmake/deploy_macos.cmake`**
- 新增 `_strip_signature(path)` 辅助函数（`codesign --remove-signature`）
- 在每个 `install_name_tool -id / -change / -add_rpath` 调用前先剥签
- 把 sentinel 创建（`file(TOUCH .../Contents/.macdeployqt.done)`）移到 codesign --deep **之前**，让 sentinel 一并被 seal

**2. `CMakeLists.txt`**
- 移除 POST_BUILD 末尾多余的 `touch sentinel` 命令（已交给 deploy_macos.cmake 在 codesign 前完成）
- macdeployqt 输出过滤里追加 grep 规则：过滤 `install_name_tool: warning: changes being made to the file will invalidate the code signature`（针对 macdeployqt 内部调用 install_name_tool 产生的同款 warning，无法剥签拦截）

### 验证

- `cmake --build build -j` 完整 deploy，零 warning
- `codesign --verify --deep --strict --verbose=2 ZoeFileManager.app`：
  - `valid on disk`
  - `satisfies its Designated Requirement`
- sentinel 跳过逻辑仍正常：第二次 build 输出 "bundle already deployed — skipping"

### 关键文件

- `cmake/deploy_macos.cmake` — `_strip_signature` 函数 + 9 处 install_name_tool 前剥签 + sentinel touch 时机
- `CMakeLists.txt` — grep 链增加 install_name_tool warning 过滤、移除多余 touch

## 长期约定（建议放 MEMORY.md）

macOS bundle deploy：
- 任何 `install_name_tool` 调用前应先 `codesign --remove-signature`，否则会输出"will invalidate"warning（虽然无害但污染日志）
- 在 bundle 内创建/修改文件必须在 `codesign --deep` **之前**，否则 `codesign --verify --deep --strict` 会失败

## PathBar 面包屑点击：复制路径 + toast 提示

需求：点击 PathBar 中任一目录段按钮时，先把对应路径复制到剪贴板，并弹出 "✓ 已复制：xxx" 的 toast 气泡，然后再跳转。

### 修改

**1. `src/PathBar.h`**
- `showCopiedToast(const QPoint&, const QString& message = QString())` 增加 message 形参，缺省时仍显示 "✓ Path copied"（兼容远程 URL 单击 fallback 路径）。

**2. `src/PathBar.cpp`**
- `showCopiedToast` 内通过 `findChild<QLabel*>("toastLabel")` 在每次显示前更新文字（toast widget 复用，但 label 文本动态）。
- `rebuildDisplay::makeCrumb` 内的 `connect(btn, &QToolButton::clicked, ...)` 改为：
  1. `QApplication::clipboard()->setText(absPath)` —— 复制 percent-encoded 原始路径（粘贴回路径栏可路由）。
  2. 远程 URL 用 `humanizeRemoteUrl(absPath)` 反解中文做提示文字；本地路径直接显示；> 80 字符截断为 `前38…后38` 防止 toast 过宽。
  3. 调 `showCopiedToast(btn->mapToGlobal(底部居中), msg)`。
  4. `emit pathSelected(absPath)` 触发 SSOT 跳转。

### 关键设计

- 复制的是 **原始 percent-encoded** 字面（`absPath`），不是反解后的中文，确保粘贴能被 router 路由。
- toast 文字用反解后的中文做提示，更友好。
- 复制 → toast → 跳转 三个动作都同步完成（toast 自身有 2s 计时器自动隐藏，不阻塞跳转）。

### 验证

- `cmake --build build -j` → 编译通过。

## 右键菜单：Open Shell Here

需求：在文件面板右键菜单增加 "Open Shell Here"，选中后在当前 active panel 的 cwd 下打开终端窗口。

### 修改

`src/MainWindow.cpp::onContextMenu`（约 2428 行 `Run Script` 之后）：
- 新增 `QAction* shellAct = menu.addAction(T("Open Shell Here"))`。
- 远程路径（`FileSystemRouter::isRemote(cwd) == true`）→ `setEnabled(false)` + tooltip 提示"only available for local paths"。本地路径连接 lambda：
  - **macOS**：`osascript -e 'tell app "Terminal" do script "cd \"<cwd>\""'`，先对路径做 `\` 和 `"` 双重转义，比 `open -a Terminal` 在含空格/中文路径上更稳。
  - **Windows**：`QProcess::startDetached("cmd.exe", {"/K"}, cwd)` —— `/K` 保留窗口，第三参数设工作目录。
  - **Linux**：探测 `x-terminal-emulator / gnome-terminal / konsole / xfce4-terminal / xterm`，分别按各家参数语法（`--working-directory=`、`--workdir`、`-e sh -c "cd ... && exec $SHELL"`）启动。全部失败时弹 information 提示。

`#include <QStandardPaths>` 已加在 QProcess 后（Linux 分支用 `findExecutable`）。

### 验证

- `cmake --build build -j` → 编译通过，零 warning。

## Open Shell Here：macOS 改用 `open -a` 修复 cwd 不生效

bug：上一版用 `osascript do script "cd <path>"` 在 macOS 打开 Terminal，但实际 shell 启动后并不在目标目录——cd 命令通过 tty 注入，时序上会被 shell 启动的 RC 文件（.zshrc / .bash_profile）顺序问题吞掉，或被 RC 里的 `cd` 语句覆盖。

修复（`MainWindow.cpp::onContextMenu` 内 Open Shell Here 的 Q_OS_MAC 分支）：
- 改用 Apple 官方推荐的 `open -a <TermApp> "<dir>"` —— 通过 Apple Event 把工作目录直接传给 Terminal/iTerm，shell 启动时 cwd 就是目标目录，不依赖命令注入。
- 优先检测 iTerm.app（`QFileInfo("/Applications/iTerm.app").exists()`）；否则回落到 Terminal.app。
- 极端情况 `open` 启动失败时仍 fallback 到旧的 osascript do script，作保守保底。

验证：`cmake --build build -j` 编译通过。

## Open Shell Here：强制新窗口而非 tab

bug：`open -a Terminal <dir>` 在 macOS Terminal.app 默认偏好下经常被路由成"在已有窗口里新增一个 tab"，与"打开完整 shell 窗口"需求不符。

修复（`MainWindow.cpp::onContextMenu` 的 macOS 分支）：
- 改用 osascript `do script "cd ..."` —— AppleScript 规则：do script 不带 `in window X` 子句时**总是创建新窗口**；带 in 子句才是在指定窗口的当前 tab 执行。
- iTerm 用 `create window with default profile command "/bin/zsh -i -c 'cd ... && exec $SHELL -i'"`，create window 一定是新窗口。
- 路径中的 `\` 和 `"` 已转义。

经验：macOS 终端"新窗口 vs 新 tab"的关键不是 `open -a` vs `osascript`，而是 AppleScript 命令是否带 `in window` 子句。要"完整新窗口"就**永远不要**带 in 子句。

验证：`cmake --build build -j` 编译通过。
